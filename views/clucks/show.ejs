<%- include('../partials/header') %>

<div class="container">
  <div class="card w-100">
    <div class="card-body">
      <h2><%= cluck.username %></h2>
      <p>
        <%= cluck.content %>
      </p>
      <p><%= cluck.image_url %></p>
      <p><strong>Created at:</strong> <%= cluck.created_at %></p>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
